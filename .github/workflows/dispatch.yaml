name: Update Submodule

on:
  repository_dispatch:
    types: [openapi-update]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code 🛎
        uses: actions/checkout@v3

      - name: Echo log
        run: |
          echo "Eventをキャッチしました"
